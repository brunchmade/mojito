language: objective-c
osx_image: xcode7.2
cache:
    - cocoapods
before_install:
    # upgrade xctool
    - "brew update"
    - "if brew outdated | grep -qx xctool; then brew upgrade xctool; fi"
    # install carthage
    - "brew gem install carthage"
    - "carthage bootstrap"
    - "carthage update"
script: >
    xctool
    -workspace Mojito.xcworkspace
    -scheme Mojito
    build test
